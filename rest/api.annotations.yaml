type: google.api.Service
config_version: 3
http:
  rules:
    - selector: pb.SearchService.SearchOrders
      post: /api/search/orders
      body: "*"
    - selector: pb.SearchService.GetOrder
      get: /api/search/orders/{id}
